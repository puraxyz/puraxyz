name: Deploy Site (deprecated — Vercel handles deployment)

# This workflow previously deployed MkDocs to GitHub Pages.
# The site is now a Next.js app in web/ deployed via Vercel.
# Keeping this file disabled for history. Delete when convenient.

on:
  workflow_dispatch:  # manual trigger only, never auto-runs

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Site deployment moved to Vercel. See web/ directory."
